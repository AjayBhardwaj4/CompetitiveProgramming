#include<iostream>
using namespace std;
 
int main() {
	int n;
	int i;
	cin >> n;										// Reading input from STDIN		// Writing output to STDOUT
	int a[n]={0};
	for(i=0;i<n;i++)
	{
		cin>>a[i];
	}
 
	int k;
	cin>>k;
	for(i=0;i<n;i++)
	{
		if(a[i]==k)
		{
			cout<<i;
		}
	}
}
